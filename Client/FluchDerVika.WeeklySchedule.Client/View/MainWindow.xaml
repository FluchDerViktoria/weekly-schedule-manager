<Window x:Class="FluchDerVika.WeeklySchedule.Client.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:FluchDerVika.WeeklySchedule.Client.View"
        xmlns:prop="clr-namespace:FluchDerVika.WeeklySchedule.Client.Properties" 
        xmlns:vm="clr-namespace:FluchDerVika.WeeklySchedule.Client.ViewModel" 
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        mc:Ignorable="d"
        Title="{x:Static prop:LanguageText.WINDOW_TITLE}" 
        Height="650" Width="1200"
        FontSize="18"
        KeyDown="Window_KeyDown">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="30"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <!-- #region Header -->
    <Grid Grid.Row="0" Background="#aa9a0089">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="80"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="80"/>
      </Grid.ColumnDefinitions>
      <Grid Grid.Column="0">
        <Button Content="Haus" Command="{Binding HomeCommand}"/>
      </Grid>
      <Grid Grid.Column="1" HorizontalAlignment="Center">
        <StackPanel Orientation="Horizontal">
          <Button Content="&lt;"/>
          <TextBlock Text="20.03 - 26.03 (KW 12)"
                     VerticalAlignment="Center"
                     Margin="5,0,5,0"/>
          <Button Content="&gt;"/>
        </StackPanel>
      </Grid>
      <Grid Grid.Column="2">
        <Button Content="Settings"/>
      </Grid>
    </Grid>
    <!-- #endregion Header -->
    <!-- #region Body -->
    <Grid Grid.Row="1">
      <ItemsControl ItemsSource="{Binding DayViewModels}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <UniformGrid Columns="7" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <view:DayView DataContext="{Binding}"/>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </Grid>
    <!-- #endregion Body -->
  </Grid>
</Window>
